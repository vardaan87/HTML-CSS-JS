// Write JavaScript program to test whether the character at the given (character) index is lowercase.

let str = "ApNaCoLlEgE";
let index = 3;

if (str[index] == str[index].toLowerCase()) {
  console.log("character is lowercase");
} else {
  console.log("character is uppercase");
}
